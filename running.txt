1. Pakai run_all.ps1

# sekali set izin eksekusi (jika perlu)
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass

# jalankan default
./scripts/run_all.ps1

# contoh lain:
./scripts/run_all.ps1 -UseSmote yes -CV 5
./scripts/run_all.ps1 -Ablation

2. Snippet catatan (copy-paste cepat per langkah)

# - Aktivasi venv
. .\.venv\Scripts\Activate.ps1

# - Feature engineering + coastline
python -m tsunami_prediction.feature_engineering --overwrite --materialize-ohe `
  --coast "data/coastline/ne_10m_coastline.shp"

# - SMOTE (pilih salah satu; kalau gagal, pakai yang minimal)
# lengkap (jika script mendukung):
python -m tsunami_prediction.smote_pipeline --datasets tectonic volcanic --all

# minimal (fallback):
python -m tsunami_prediction.smote_pipeline

# - Stacking (train + threshold tuning)
# pakai SMOTE otomatis bila file SMOTE ada
python -m tsunami_prediction.stacking_pipeline --datasets tectonic volcanic `
  --cv 3 --with-xgb --meta-grid --feature-select none --use-smote auto

# atau paksa SMOTE / non-SMOTE
python -m tsunami_prediction.stacking_pipeline --datasets tectonic volcanic `
  --cv 5 --with-xgb --meta-grid --feature-select none --use-smote yes

# -Run experiments (configs.yaml)
python experiments/run_experiment.py --config experiments/configs.yaml

# - Jalankan UI
python interface/app.py

# - Git (opsional)
git add -A
git commit -m "chore: refresh FE+SMOTE+stacking after coastline update"
git push -u origin main --tags